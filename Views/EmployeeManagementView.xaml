<UserControl x:Class="QuanLyNhanSu_WPF.Views.EmployeeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:QuanLyNhanSu_WPF.Helpers">
    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Danh Sách Nhân Sự" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueDarkBrush}"/>
                <TextBlock Text="Quản lý hồ sơ, thông tin và tài khoản của toàn bộ nhân viên" Opacity="0.6" FontSize="13"/>
            </StackPanel>
        </DockPanel>

        <!-- Search and Actions -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,24" materialDesign:ElevationAssist.Elevation="Dp2">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <materialDesign:PackIcon Kind="Search" VerticalAlignment="Center" Margin="0,0,12,0" Opacity="0.5" Width="20" Height="20"/>
                    <TextBox Width="300" materialDesign:HintAssist.Hint="Tìm theo tên, mã hoặc email..." 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Command="{Binding AddCommand}" Style="{StaticResource MaterialDesignFlatButton}"
                            Content="➕ THÊM MỚI" Visibility="{Binding CanAdd, Converter={StaticResource BoolToVis}}"/>
                    <Button Command="{Binding RefreshCommand}" Style="{StaticResource MaterialDesignFlatButton}"
                            Content="🔄 LÀM MỚI" Margin="8,0,0,0"/>
                    <Button Command="{Binding ExportCommand}" Style="{StaticResource MaterialDesignFlatButton}"
                            Content="📥 XUẤT EXCEL" Margin="8,0,0,0" Foreground="Green"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>

        <!-- DataGrid -->
        <materialDesign:Card Grid.Row="1" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <DataGrid ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}"
                          AutoGenerateColumns="False" IsReadOnly="True" 
                          materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="8"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Ảnh" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Ellipse Width="32" Height="32" Fill="{DynamicResource PrimaryHueLightBrush}">
                                        <!-- Placeholder or Image binding would go here -->
                                    </Ellipse>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Mã NV" Binding="{Binding Code}" Width="100"/>
                        <DataGridTextColumn Header="Họ và Tên" Binding="{Binding Name}" Width="200"/>
                        <DataGridTextColumn Header="Phòng ban" Binding="{Binding Department.DeptName}" Width="150"/>
                        <DataGridTextColumn Header="Chức vụ" Binding="{Binding Position.PosName}" Width="150"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100"/>
                        <DataGridTemplateColumn Header="Thao tác" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                                Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                ToolTip="Chỉnh sửa" Width="30" Height="30">
                                            <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                                Command="{Binding DataContext.CreateUserAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                ToolTip="Tạo tài khoản" Foreground="Green" Width="30" Height="30">
                                            <materialDesign:PackIcon Kind="AccountPlus" Width="16" Height="16"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignFlatButton}" 
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                ToolTip="Vô hiệu hóa" Foreground="Red" Width="30" Height="30">
                                            <materialDesign:PackIcon Kind="Delete" Width="16" Height="16"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading Overlay inside Card -->
                <ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                             VerticalAlignment="Top" Style="{StaticResource MaterialDesignLinearProgressBar}"/>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
