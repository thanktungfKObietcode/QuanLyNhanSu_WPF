<UserControl x:Class="QuanLyNhanSu_WPF.Views.SalaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:QuanLyNhanSu_WPF.Helpers">
    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:InverseBoolToVisibilityConverter x:Key="InvBoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Padding="16" Margin="0,0,0,16" materialDesign:ElevationAssist.Elevation="Dp2">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <ComboBox materialDesign:HintAssist.Hint="Thang" ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Width="80" Margin="0,0,12,0"/>
                    <ComboBox materialDesign:HintAssist.Hint="Nam" ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="100" Margin="0,0,12,0"/>
                    <ComboBox materialDesign:HintAssist.Hint="Nhan vien"
                              Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}"
                              ItemsSource="{Binding Employees}" DisplayMemberPath="Name"
                              SelectedItem="{Binding SelectedEmployeeFilter}" Width="220" Margin="0,0,12,0"/>
                    <Button Command="{Binding LoadCommand}" Style="{StaticResource MaterialDesignFlatButton}" Content="TAI DU LIEU" Margin="0,0,12,0"/>
                    <Button Command="{Binding ExportDailyCommand}" Style="{StaticResource MaterialDesignFlatButton}" Content="XUAT LUONG NGAY" Margin="0,0,12,0"/>
                    <Button Command="{Binding ExportMonthlyCommand}" Style="{StaticResource MaterialDesignFlatButton}" Content="XUAT LUONG THANG"/>
                </StackPanel>

                <Button Command="{Binding CalculateSalaryCommand}" DockPanel.Dock="Right"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}"
                        Style="{StaticResource MaterialDesignRaisedButton}" Background="#C2410C" BorderBrush="#C2410C"
                        Content="CHOT LUONG THANG"/>
            </DockPanel>
        </materialDesign:Card>

        <Grid Grid.Row="1">
            <TabControl>
                <TabItem Header="Luong Ngay">
                    <materialDesign:Card Margin="0,8,0,0" Padding="8">
                        <Grid>
                            <DataGrid ItemsSource="{Binding DailySalaryEntries}" SelectedItem="{Binding SelectedDailySalaryEntry}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Ngay" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="100"/>
                                    <DataGridTextColumn Header="Nhan vien" Binding="{Binding EmployeeName}" Width="180"/>
                                    <DataGridTextColumn Header="Trang thai" Binding="{Binding AttendanceStatus}" Width="110"/>
                                    <DataGridTextColumn Header="Cong" Binding="{Binding WorkUnits, StringFormat={}{0:N2}}" Width="80"/>
                                    <DataGridTextColumn Header="Gio lam" Binding="{Binding WorkingHours, StringFormat={}{0:N2}}" Width="90"/>
                                    <DataGridTextColumn Header="Tang ca" Binding="{Binding OvertimeHours, StringFormat={}{0:N1}}" Width="80"/>
                                    <DataGridTextColumn Header="Tong nhan" Binding="{Binding TotalAmount, StringFormat={}{0:N0} d}" Width="130"/>
                                    <DataGridTextColumn Header="Ghi chu" Binding="{Binding Note}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" VerticalAlignment="Top"/>
                        </Grid>
                    </materialDesign:Card>
                </TabItem>

                <TabItem Header="Luong Thang Tam Tinh">
                    <materialDesign:Card Margin="0,8,0,0" Padding="8">
                        <Grid>
                            <DataGrid ItemsSource="{Binding MonthlySalarySummaries}" SelectedItem="{Binding SelectedMonthlySalarySummary}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Nhan vien" Binding="{Binding EmployeeName}" Width="180"/>
                                    <DataGridTextColumn Header="Ma NV" Binding="{Binding EmployeeCode}" Width="90"/>
                                    <DataGridTextColumn Header="Phong ban" Binding="{Binding DepartmentName}" Width="140"/>
                                    <DataGridTextColumn Header="Tong cong" Binding="{Binding TotalWorkUnits, StringFormat={}{0:N2}}" Width="90"/>
                                    <DataGridTextColumn Header="Tong gio lam" Binding="{Binding TotalWorkingHours, StringFormat={}{0:N2}}" Width="100"/>
                                    <DataGridTextColumn Header="Tong OT" Binding="{Binding TotalOvertimeHours, StringFormat={}{0:N1}}" Width="90"/>
                                    <DataGridTextColumn Header="Luong theo ngay" Binding="{Binding DailySalaryTotal, StringFormat={}{0:N0} d}" Width="130"/>
                                    <DataGridTextColumn Header="Luong OT" Binding="{Binding OvertimeSalaryTotal, StringFormat={}{0:N0} d}" Width="110"/>
                                    <DataGridTextColumn Header="Phu cap" Binding="{Binding FixedAllowances, StringFormat={}{0:N0} d}" Width="110"/>
                                    <DataGridTextColumn Header="Tong tam tinh" Binding="{Binding ProjectedNetSalary, StringFormat={}{0:N0} d}" Width="130"/>
                                    <DataGridTextColumn Header="Luong da chot" Binding="{Binding OfficialNetSalary, StringFormat={}{0:N0} d}" Width="130"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" VerticalAlignment="Top"/>
                        </Grid>
                    </materialDesign:Card>
                </TabItem>

                <TabItem Header="Bang Luong Thang">
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="320"/>
                        </Grid.ColumnDefinitions>

                        <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="8">
                            <Grid>
                                <DataGrid ItemsSource="{Binding Salaries}" SelectedItem="{Binding SelectedSalary}" AutoGenerateColumns="False" IsReadOnly="True">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Nhan vien" Binding="{Binding Employee.Name}" Width="170"/>
                                        <DataGridTextColumn Header="Ma NV" Binding="{Binding Employee.Code}" Width="90"/>
                                        <DataGridTextColumn Header="Luong co ban" Binding="{Binding BaseSalary, StringFormat={}{0:N0} d}" Width="120"/>
                                        <DataGridTextColumn Header="Phu cap" Binding="{Binding Allowances, StringFormat={}{0:N0} d}" Width="110"/>
                                        <DataGridTextColumn Header="KPI" Binding="{Binding KPIBonus, StringFormat={}{0:N0} d}" Width="100"/>
                                        <DataGridTextColumn Header="Hoa hong" Binding="{Binding Commission, StringFormat={}{0:N0} d}" Width="100"/>
                                        <DataGridTextColumn Header="Luong OT" Binding="{Binding OTSalary, StringFormat={}{0:N0} d}" Width="100"/>
                                        <DataGridTextColumn Header="Thuc linh" Binding="{Binding NetSalary, StringFormat={}{0:N0} d}" Width="120"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" VerticalAlignment="Top"/>
                            </Grid>
                        </materialDesign:Card>

                        <materialDesign:Card Grid.Column="1" Margin="8,0,0,0" Padding="16">
                            <StackPanel>
                                <TextBlock Text="Chi Tiet Luong Thang" FontWeight="Bold" FontSize="16" Margin="0,0,0,16"/>
                                <TextBlock Text="Chon mot dong trong bang luong de xem chi tiet."
                                           Opacity="0.6"
                                           Margin="0,0,0,12">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedSalary}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding SelectedSalary}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedSalary}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <DockPanel Margin="0,0,0,8">
                                        <TextBlock Text="Nhan vien"/>
                                        <TextBlock Text="{Binding SelectedSalary.Employee.Name, TargetNullValue='--'}" FontWeight="Bold" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <DockPanel Margin="0,0,0,8">
                                        <TextBlock Text="Ky luong"/>
                                        <TextBlock DockPanel.Dock="Right" FontWeight="Bold">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}Thang {0}/{1}">
                                                    <Binding Path="SelectedSalary.Month" TargetNullValue="--"/>
                                                    <Binding Path="SelectedSalary.Year" TargetNullValue="--"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DockPanel>
                                    <Separator Margin="0,8"/>
                                    <DockPanel Margin="0,4">
                                        <TextBlock Text="Luong co ban"/>
                                        <TextBlock Text="{Binding SelectedSalary.BaseSalary, StringFormat={}{0:N0} d, TargetNullValue='--'}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <DockPanel Margin="0,4">
                                        <TextBlock Text="Phu cap"/>
                                        <TextBlock Text="{Binding SelectedSalary.Allowances, StringFormat={}{0:N0} d, TargetNullValue='--'}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <DockPanel Margin="0,4">
                                        <TextBlock Text="KPI"/>
                                        <TextBlock Text="{Binding SelectedSalary.KPIBonus, StringFormat={}{0:N0} d, TargetNullValue='--'}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <DockPanel Margin="0,4">
                                        <TextBlock Text="Hoa hong"/>
                                        <TextBlock Text="{Binding SelectedSalary.Commission, StringFormat={}{0:N0} d, TargetNullValue='--'}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <DockPanel Margin="0,4">
                                        <TextBlock Text="Luong OT"/>
                                        <TextBlock Text="{Binding SelectedSalary.OTSalary, StringFormat={}{0:N0} d, TargetNullValue='--'}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <DockPanel Margin="0,4">
                                        <TextBlock Text="Khau tru"/>
                                        <TextBlock Text="{Binding SelectedSalary.Deductions, StringFormat={}{0:N0} d, TargetNullValue='--'}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                    <Separator Margin="0,12"/>
                                    <materialDesign:Card Background="{DynamicResource PrimaryHueDarkBrush}" Padding="12">
                                        <DockPanel>
                                            <TextBlock Text="Thuc linh" Foreground="White" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding SelectedSalary.NetSalary, StringFormat={}{0:N0} d, TargetNullValue='--'}" Foreground="White" FontWeight="Bold" DockPanel.Dock="Right"/>
                                        </DockPanel>
                                    </materialDesign:Card>
                                </StackPanel>
                            </StackPanel>
                        </materialDesign:Card>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
