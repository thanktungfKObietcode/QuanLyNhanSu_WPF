<Window x:Class="QuanLyNhanSu_WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:helpers="clr-namespace:QuanLyNhanSu_WPF.Helpers"
        Title="Đăng nhập - Quản Lý Nhân Sự" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <ResourceDictionary>
            <helpers:NullOrEmptyToVisibilityConverter x:Key="NullToVis"/>
            <helpers:BoolToVisibilityConverter x:Key="BoolToVis"/>
            <Style x:Key="LoginTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                <Setter Property="Margin" Value="0,0,0,16"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <!-- Background Gradient -->
        <Border>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#4F46E5" Offset="0"/>
                    <GradientStop Color="#06B6D4" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Decorative Circles -->
        <Canvas IsHitTestVisible="False">
            <Ellipse Width="300" Height="300" Fill="White" Opacity="0.05" Canvas.Left="-100" Canvas.Top="-100"/>
            <Ellipse Width="200" Height="200" Fill="White" Opacity="0.05" Canvas.Right="-50" Canvas.Bottom="-50"/>
        </Canvas>

        <Border CornerRadius="24" Background="White" Width="400" Height="520" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" Direction="-90" RenderingBias="Quality" ShadowDepth="10" Opacity="0.15"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Close Button -->
                <Button Style="{StaticResource MaterialDesignFlatButton}" 
                        Content="✕" Width="40" Height="40" Padding="0" Click="CloseButton_Click"
                        HorizontalAlignment="Right" Margin="16" Foreground="#9CA3AF"/>

                <StackPanel Grid.Row="1" Margin="48,0,48,48">
                    <materialDesign:PackIcon Kind="AccountTie" Width="72" Height="72" 
                                             HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,16"/>
                    
                    <TextBlock Text="HR MASTER" FontSize="24" FontWeight="Black" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock Text="Quản lý nhân sự chuyên nghiệp" FontSize="12" Opacity="0.5" HorizontalAlignment="Center" Margin="0,4,0,32"/>

                    <TextBox materialDesign:HintAssist.Hint="Tên đăng nhập" 
                             materialDesign:TextFieldAssist.HasClearButton="True"
                             materialDesign:TextFieldAssist.PrefixText="👤 "
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,16">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <PasswordBox x:Name="PwdBox" materialDesign:HintAssist.Hint="Mật khẩu" 
                                 materialDesign:TextFieldAssist.PrefixText="🔒 "
                                 Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                 PasswordChanged="PwdBox_PasswordChanged"
                                 Margin="0,0,0,8">
                    </PasswordBox>

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" 
                               FontSize="12" Margin="0,4,0,8" HorizontalAlignment="Left"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVis}}"
                               TextWrapping="Wrap">
                    </TextBlock>

                    <Button Content="ĐĂNG NHẬP" Command="{Binding LoginCommand}" 
                            Height="50" FontWeight="Bold" FontSize="14"
                            materialDesign:ButtonAssist.CornerRadius="12" Margin="0,16,0,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <ProgressBar IsIndeterminate="True" Margin="0,16,0,0" Height="4"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
                    </ProgressBar>
                    
                    <TextBlock Text="Quên mật khẩu?" FontSize="12" Foreground="{DynamicResource PrimaryHueMidBrush}" 
                               HorizontalAlignment="Center" Margin="0,24,0,0" Cursor="Hand" Opacity="0.8"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>