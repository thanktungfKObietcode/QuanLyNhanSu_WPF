<UserControl x:Class="QuanLyNhanSu_WPF.Views.LeaveRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:QuanLyNhanSu_WPF.Helpers"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:InverseBoolToVisibilityConverter x:Key="InvBoolToVis"/>
        <helpers:EnumToVietnameseConverter x:Key="EnumToVN"/>
        <helpers:LeaveTypeToVietnameseConverter x:Key="LeaveTypeToVN"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" materialDesign:ElevationAssist.Elevation="Dp1">
            <Grid>
                <TabControl Style="{StaticResource MaterialDesignTabControl}">
                    <TabItem Header="CHỜ DUYỆT" Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                        <DataGrid ItemsSource="{Binding PendingRequests}" SelectedItem="{Binding SelectedRequest}"
                                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,8">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nhân viên" Binding="{Binding Employee.Name}" Width="150"/>
                                <DataGridTextColumn Header="Loại" Binding="{Binding LeaveType, Converter={StaticResource LeaveTypeToVN}}" Width="90"/>
                                <DataGridTextColumn Header="Bắt đầu" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Kết thúc" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Lý do" Binding="{Binding Reason}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="TẤT CẢ" Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                        <DataGrid ItemsSource="{Binding AllRequests}" SelectedItem="{Binding SelectedRequest}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,8">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nhân viên" Binding="{Binding Employee.Name}" Width="120"/>
                                <DataGridTextColumn Header="Loại" Binding="{Binding LeaveType, Converter={StaticResource LeaveTypeToVN}}" Width="90"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status, Converter={StaticResource EnumToVN}}" Width="100"/>
                                <DataGridTextColumn Header="Bắt đầu" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="90"/>
                                <DataGridTextColumn Header="Kết thúc" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="90"/>
                                <DataGridTextColumn Header="Ghi chú duyệt" Binding="{Binding ApprovalNote}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="ĐƠN CỦA TÔI" Visibility="{Binding IsAdmin, Converter={StaticResource InvBoolToVis}}">
                        <DataGrid ItemsSource="{Binding MyRequests}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,8">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Loại" Binding="{Binding LeaveType, Converter={StaticResource LeaveTypeToVN}}" Width="90"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status, Converter={StaticResource EnumToVN}}" Width="100"/>
                                <DataGridTextColumn Header="Từ ngày" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Đến ngày" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Ghi chú Admin" Binding="{Binding ApprovalNote}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
                <ProgressBar IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" VerticalAlignment="Top"/>
            </Grid>
        </materialDesign:Card>

        <StackPanel Grid.Column="1" Margin="8,0,0,0">
            <materialDesign:Card Padding="16" Visibility="{Binding IsAdmin, Converter={StaticResource InvBoolToVis}}" materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <TextBlock Text="TẠO ĐƠN NGHỈ PHÉP" FontWeight="Bold" Margin="0,0,0,16"/>
                    <ComboBox materialDesign:HintAssist.Hint="Loại nghỉ phép" ItemsSource="{Binding LeaveTypes}" SelectedItem="{Binding NewLeaveType}" Margin="0,0,0,12"/>
                    <DatePicker materialDesign:HintAssist.Hint="Từ ngày" SelectedDate="{Binding NewStartDate}" Margin="0,0,0,12"/>
                    <DatePicker materialDesign:HintAssist.Hint="Đến ngày" SelectedDate="{Binding NewEndDate}" Margin="0,0,0,12"/>

                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="{Binding RemainingAnnualLeaveDays, StringFormat=Số ngày phép năm còn lại: {0}}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding RequestedLeaveDays, StringFormat=Số ngày đang đăng ký: {0}}" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <TextBox materialDesign:HintAssist.Hint="Lý do" Text="{Binding NewReason}" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,0,0,16"/>
                    <Button Command="{Binding SubmitRequestCommand}" Content="GỬI ĐƠN XIN NGHỈ"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Padding="16" Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}" materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <TextBlock Text="DUYỆT ĐƠN NGHỈ" FontWeight="Bold" Margin="0,0,0,16"/>
                    <TextBlock Text="Đang chọn:" Opacity="0.5" FontSize="11"/>
                    <TextBlock Text="{Binding SelectedRequest.Employee.Name, FallbackValue='Chưa chọn đơn'}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

                    <TextBox materialDesign:HintAssist.Hint="Ghi chú duyệt" Text="{Binding ApprovalNotes}" TextWrapping="Wrap" AcceptsReturn="True" Height="60" Margin="0,0,0,16"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Command="{Binding RejectCommand}" Content="TỪ CHỐI" Style="{StaticResource MaterialDesignFlatButton}" Foreground="Red" Margin="0,0,4,0"/>
                        <Button Grid.Column="1" Command="{Binding ApproveCommand}" Content="DUYỆT" Style="{StaticResource MaterialDesignRaisedButton}" Background="Green" BorderBrush="Green" Margin="4,0,0,0"/>
                    </Grid>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
</UserControl>
